<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.riptidez.notebackend.mapper.student.StudentMapper">

    <resultMap id="StudentResultMap" type="Student">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="className" column="class_name"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
    </resultMap>

    <select id="selectAll" resultMap="StudentResultMap">
        SELECT * FROM student ORDER BY id
    </select>

    <select id="selectById" parameterType="int" resultMap="StudentResultMap">
        SELECT * FROM student WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="Student" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO student (name, age, class_name)
        VALUES (#{name}, #{age}, #{className})
    </insert>

    <update id="update" parameterType="Student">
        UPDATE student
        SET name = #{name}, age = #{age}, class_name = #{className}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        DELETE FROM student WHERE id = #{id}
    </delete>
</mapper>