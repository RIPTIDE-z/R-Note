<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <!-- 开启驼峰映射 -->
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <!-- 启用日志 -->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
    </settings>

    <!-- 别名-->
    <typeAliases>
        <typeAlias alias="User" type="com.riptidez.notebackend.auth.entity.User"/>
        <typeAlias alias="Note" type="com.riptidez.notebackend.note.entity.Note"/>
        <typeAlias alias="NoteHistory" type="com.riptidez.notebackend.note.entity.NoteHistory"/>
    </typeAliases>

</configuration>
